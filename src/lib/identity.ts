// The observatory's identity links, in one place.
// While null, the footer and the About page simply omit the corresponding
// link (no dead placeholders).
export const LINKEDIN_URL: string | null = "https://www.linkedin.com/in/matthias-duflos-69099a329";
export const CONTACT_EMAIL: string | null = null; // e.g. "hello@…"
