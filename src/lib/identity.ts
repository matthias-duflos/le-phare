// The observatory's identity links, in one place.
// Fill these before sharing the site; while null, the footer and the
// About page simply omit the corresponding link (no dead placeholders).
export const LINKEDIN_URL: string | null = null; // e.g. "https://www.linkedin.com/in/…"
export const CONTACT_EMAIL: string | null = null; // e.g. "hello@…"
